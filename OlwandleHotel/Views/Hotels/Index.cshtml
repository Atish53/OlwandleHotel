@model IEnumerable<OlwandleHotel.Models.Hotel>

@{
    ViewBag.Title = "Index";
}

<h2 class="text-info text-center">Our Hotels</h2>

@if (User.IsInRole("Admin"))
{
    <p>
        @Html.ActionLink("Create New Event", "Create")
    </p>
}

<div class="container">
    <br />
    <p class="text-success text-center">Luxury-End Hotel Offerings.</p>
    <hr>
    <div class="row">
        @foreach (var item in Model.Where(x => x.HotelName == "Luxury Hotel"))
        {
            <div class="col-md-4">
                <a href="#" data-toggle="modal" data-target="#@item.HotelId">
                    <img src="data:image;base64,@System.Convert.ToBase64String(item.HotelPicture)" class="img-responsive img-rounded" width="100%" height="100%" style="min-width: 360px; max-width: 360px; width: 360px; min-height: 200px; max-height: 200px; height: 200px; background-size: contain;" />
                </a>

                <!-- Modal -->
                <div class="modal fade" id="@item.HotelId" role="dialog">
                    <div class="modal-dialog">

                        <!-- Modal content-->
                        <div class="modal-content">
                            <div class="modal-header">
                                <button type="button" class="close" data-dismiss="modal">&times;</button>
                                <h4 class="modal-title text-center">@item.HotelLocation</h4>
                            </div>
                            <div class="modal-body">
                                <img src="data:image;base64,@System.Convert.ToBase64String(item.HotelPicture)" width="100%" height="100%" />
                                <br />
                                <br />                               

                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-danger" onclick="location.href='@Url.Action("Details", "Events", new { id = item.HotelId })'">View Available Rooms</button>
                                <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        }
    </div>
</div>


<div class="container">
    <br />
    <p class="text-success text-center">Standard-Level Hotel Offerings.</p>
    <hr>
    <div class="row">
        @foreach (var item in Model.Where(x => x.HotelName == "Standard Hotel"))
        {
            <div class="col-md-4">
                <a href="#" data-toggle="modal" data-target="#@item.HotelId">
                    <img src="data:image;base64,@System.Convert.ToBase64String(item.HotelPicture)" class="img-responsive img-rounded" width="100%" height="100%" style="min-width: 360px; max-width: 360px; width: 360px; min-height: 200px; max-height: 200px; height: 200px; background-size: contain;" />
                </a>

                <!-- Modal -->
                <div class="modal fade" id="@item.HotelId" role="dialog">
                    <div class="modal-dialog">

                        <!-- Modal content-->
                        <div class="modal-content">
                            <div class="modal-header">
                                <button type="button" class="close" data-dismiss="modal">&times;</button>
                                <h4 class="modal-title text-center">@item.HotelLocation</h4>
                            </div>
                            <div class="modal-body">
                                <img src="data:image;base64,@System.Convert.ToBase64String(item.HotelPicture)" width="100%" height="100%" />
                                <br />
                                <br />                                

                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-danger" onclick="location.href='@Url.Action("Details", "Events", new { id = item.HotelId })'">View Available Rooms</button>
                                <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        }
    </div>
</div>


<div class="container">
    <br />
    <p class="text-success text-center">Budget-Level Hotel Offerings.</p>
    <hr>
    <div class="row">
        @foreach (var item in Model.Where(x => x.HotelName == "Budget Hotel"))
        {
            <div class="col-md-4">
                <a href="#" data-toggle="modal" data-target="#@item.HotelId">
                    <img src="data:image;base64,@System.Convert.ToBase64String(item.HotelPicture)" class="img-responsive img-rounded" width="100%" height="100%" style="min-width: 360px; max-width: 360px; width: 360px; min-height: 200px; max-height: 200px; height: 200px; background-size: contain;" />
                </a>

                <!-- Modal -->
                <div class="modal fade" id="@item.HotelId" role="dialog">
                    <div class="modal-dialog">

                        <!-- Modal content-->
                        <div class="modal-content">
                            <div class="modal-header">
                                <button type="button" class="close" data-dismiss="modal">&times;</button>
                                <h4 class="modal-title text-center">@item.HotelLocation</h4>
                            </div>
                            <div class="modal-body">
                                <img src="data:image;base64,@System.Convert.ToBase64String(item.HotelPicture)" width="100%" height="100%" />
                                <br />
                                <br />                               

                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-danger" onclick="location.href='@Url.Action("Details", "Events", new { id = item.HotelId })'">View Available Rooms</button>
                                <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        }
    </div>
</div>